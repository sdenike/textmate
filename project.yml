# generated by bin/rave2yaml --emit-yaml text -- do not hand-edit
---
name: TextMate
options:
  createIntermediateGroups: true
configFiles:
  Debug: Xcode/Base.xcconfig
  Release: Xcode/Base.xcconfig
settings:
  base:
    CLANG_CXX_LANGUAGE_STANDARD: c++20
    GCC_C_LANGUAGE_STANDARD: c99
targets:
  text:
    type: library.static
    platform: macOS
    sources:
      - path: "Frameworks/text/src"
    settings:
      base:
        GCC_PREFIX_HEADER: "$(SRCROOT)/Shared/PCH/prelude.cc"
  text_test:
    type: tool
    platform: macOS
    sources:
      - path: "$(DERIVED_FILE_DIR)/_Ttext.cc"
        optional: true
        buildPhase: sources
    preBuildScripts:
      - script: "\"$SRCROOT/Xcode/scripts/gen_test.sh\" text"
        name: "Generate CxxTest runner (text)"
        outputFiles:
          - "$(DERIVED_FILE_DIR)/_Ttext.cc"
    settings:
      base:
        GCC_PREFIX_HEADER: "$(SRCROOT)/Shared/PCH/prelude.cc"
        HEADER_SEARCH_PATHS: "$(inherited) $(SRCROOT)/Xcode/include/text"
    dependencies:
      - target: text
      - sdk: CoreFoundation.framework
      - sdk: libiconv.tbd
